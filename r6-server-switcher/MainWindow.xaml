<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="r6_server_switcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:r6_server_switcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="r6-server-switcher">

    <Grid Background="{StaticResource PaletteDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="R6 Server Switcher" Foreground="{StaticResource PaletteLight}" Background="{StaticResource PaletteDark}">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xe701;" Foreground="{StaticResource PaletteAccent}"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="rootFrame" Grid.Row="1" SourcePageType="local:MainPage"/>
    </Grid>
</Window>
